# C-Claw 项目指南

## 协作规则

### 1. 修改需确认

- 对话结束后，**不要直接触发修改**，需等待我明确确认后再执行。
- 只有当我明确说"修改"、"执行"、"开始改"、"动手吧"等指令时，才进行实际的文件修改。
- 在此之前可以分析、规划、展示方案，但不要落笔。

### 2. 多用 Sub-agent 控制上下文

- 尽量使用 `Agent` 工具派发子代理执行复杂、多步骤的任务，避免主对话上下文膨胀。
- 适合派发的场景：代码搜索、多文件分析、研究类任务等。
- 子代理返回的结果应简洁汇总，避免原文冗长回传。

### 3. 任务完成标准（铁律）

- 任何任务/Phase 的完成标准是**四项全部通过**才算完成：
  1. **编译通过**：`mvn compile`（Java）或 `npm run build`（Electron）无错误
  2. **代码评审通过**：经 `/code-review` 检查无严重问题
  3. **测试通过**：`mvn test`（Java）或 `npm test`（Electron）全部绿灯
  4. **功能验收通过**：逐项对照计划中的交付物，确认每个功能点均已实现且可运行
- 四项中任一未通过，任务不得标记为完成。